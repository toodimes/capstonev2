<header>
  <nav id="sticktop" class="navbar navbar-default">
    <div class="container">
      <div class="navbar-header">
        <% if current_user %>
            <a class="navbar-brand" href="/<%= current_user.trainer_application ? 'trainer_profiles' : 'user_profiles' %>/<%= current_user.id %>"><%= current_user.email %>(<%= current_user.id %>)</a>
        <% else %>
            <a class="navbar-brand" href="/">NoRxcuses</a>
        <% end %>
      </div>
        
      <div id="dl-menu" class="xv-menuwrapper responsive-menu">
          <button class="dl-trigger">Open Menu</button>
          <div class="clearfix"></div>
          <ul class="dl-menu">
              <% if current_user %>
                <li class="parent"><a href="/messages">Inbox</a></li>
                <li><a href="/logout">Logout</a></li>
                <li><a href="/<%= current_user.trainer_application ? 'trainer_profiles' : 'user_profiles' %>/<%= current_user.id %>">My Profile</a></li>
            <% else %>
                <li><a href="/users/sign_in">Login</a></li>
            <% end %>              
            <li class="parent"><a href="/exercises">Exercises</a>
              <ul class="lg-submenu">
               <!-- <li><a>Muscles</a> -->
                <ul class="lg-submenu" style="list-style: none">
                  <li><figure><img src="http://purplehippodesign.co.uk/wp-content/uploads/2016/06/favicon.png" style="width: 0px; height: 0px; list-style: none"/></figure></li>
                </ul>
               </li>
              </ul>
            </li>
          </ul>
      </div><!-- /dl-menuwrapper -->
    </div>
  </nav>
</header>