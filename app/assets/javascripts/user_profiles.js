/* global Vue */
document.addEventListener("DOMContentLoaded", function(event) {
  var UserProfileShowApp = new Vue({
    el: '#user-profile-show-app',
    data: {
      message: "Hello Show User Profile!"
    },
  });
  
});
