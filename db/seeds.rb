# This file should contain all the record creation needed to seed the database with its default values.
# The data can then be loaded with the rails db:seed command (or created alongside the database with db:setup).
#
# Examples:
#
#   movies = Movie.create([{ name: 'Star Wars' }, { name: 'Lord of the Rings' }])
#   Character.create(name: 'Luke', movie: movies.first)
# app = [true, false]
# 25.times do 
#   User.create(email: Faker::Internet.email, password: "password", password_confirmation: "password", trainer_application: app.sample)
# end
# trainers = User.where(is_trainer: true)
# User.where(trainer_id: 12).each do |user|
#   user.update(trainer_id: trainers.sample.id)
# end
# User.all.each do |user|
#   5.times do 
#     Goal.create(name: Faker::HarryPotter.quote, user_id: user.id, completed: app.sample)
#   end
# end


puts "Done!"
